# Archive Notes

This directory contains historical and non-authoritative material kept for traceability only.

Use these instead for current truth:

- `docs/SMOKE_DEMO.md` for the authoritative smoke/demo sequence
- `docs/rfq_manager_ms_openapi_current.yaml` for the authoritative API contract
- `README.md`, `docs/RUNBOOK.md`, and `docs/ARCHITECTURE_ONE_PAGER.md` for the current operational narrative

Archive guidance:

- Files here may describe earlier plans, earlier contracts, or exploratory UI concepts.
- `docs/archive/node_modules/` is a legacy archived bundle and is not part of the active product/runtime path.
- Nothing in this folder should be treated as a source of current product truth unless it is explicitly referenced by an active document for historical context.
