# CLEANUP_PROGRESS

Last updated: 2026-03-19 (IF-01 + IF-02 + IF-03 implemented; IF-04 verified)
Repository: `rfq_manager_ms`
Execution mode: phase-ordered, verify-first, minimal-scope

## Execution Order (locked)
1. BR-01
2. FS-01
3. MG-01
4. LG-01
5. LG-02
6. LG-03 + LG-04
7. SC-01 + SC-02 + SC-03
8. MD-01 + LG-05
9. MD-02 + MD-03 + MD-04
10. TS-01 + TS-02 + TS-03
11. DC-01 + DC-02 + DC-03 + SD-01 + SD-02 + SD-03
12. IF-01 + IF-02 + IF-03 + IF-04

## Issue Matrix

| Issue ID | Title | Status | Summary of Work Done | Files Touched | Verification Result | Commit Hash | Notes / Remaining Risk |
|---|---|---|---|---|---|---|---|
| BR-01 | 6 unmerged branches — main is stale | RESOLVED | Merged `feature/regenerate-current-documentation` into `main` (contains full chain). Resolved one docs conflict conservatively by keeping archived implementation-plan file from branch tip. Fast-forwarded `develop` to updated `main` locally. Published dedicated PR source branch `chore/br-01-cleanup-baseline` to origin. | README.md; docs/archive/*; docs/*_current.*; src/*; tests/* | Verified by `git rev-list --left-right --count main...<branch>`: all six chain branches show `N 0` (main ahead, branch not ahead). Graph shows merge commit on `main`; `develop` ff-updated locally. | cb12441 | BR-01 baseline code is ready on `origin/chore/br-01-cleanup-baseline`. Automated PR creation from tooling is blocked by missing GitKraken/GitHub auth; PR URL prepared: `https://github.com/DarkyDepths/rfq_manager_ms/pull/new/chore/br-01-cleanup-baseline`. No FS-01+ work started. |
| FS-01 | File storage write/read path coherence | RESOLVED | Verified on merged `main` baseline with deterministic end-to-end round-trip: upload to stage, DB metadata persistence, physical file write, list, download, legacy `uploads/` prefix fallback, delete + post-delete download rejection. No production-code defect reproduced. Added focused FS-01 integration coverage. | tests/integration/test_fs01_file_roundtrip.py | `pytest tests/integration/test_fs01_file_roundtrip.py tests/unit/test_file_controller.py -q` → 9 passed. Upload/download/delete and missing-file 404 behavior all confirmed. | - | Verified in SQLite-backed automated flow with FastAPI endpoints and real controllers/datasources. Residual risk: did not execute against live PostgreSQL instance in this environment due DB connection encoding error in local `.env` runtime. |
| MG-01 | Alembic migrations are non-functional | RESOLVED | Added authoritative schema migration (`bc8fe52aaace`) superseding legacy no-op head (`phase2_filter_indexes`). New revision creates all 11 tables, 21 model-backed indexes, and the cyclic `rfq.current_stage_id -> rfq_stage.id` FK (PostgreSQL path + SQLite batch-compatible path). Updated seed bootstrap to use Alembic (`downgrade base`/`upgrade head`) instead of `Base.metadata.create_all/drop_all`. | migrations/versions/bc8fe52aaace_authoritative_schema_baseline.py; migrations/versions/phase2_filter_indexes.py; scripts/seed.py | Verified with clean DB flow: `alembic upgrade head` created full schema (11/11 expected tables, 0 missing, 0 extra; 21 non-auto indexes), `alembic downgrade base` succeeded, and `alembic upgrade head` succeeded again. `scripts/seed.py --scenario=minimal --reset` successfully runs through Alembic migrations before seeding. | - | Local direct PostgreSQL validation remains constrained in this environment (Docker engine unavailable; direct psycopg2 connect raises UnicodeDecodeError). Migration logic preserves PostgreSQL-native FK path and was validated structurally plus executable in clean disposable DB flow. |
| LG-01 | Missing RFQ status FSM enforcement | RESOLVED | Merged to `main` with explicit transition map in RFQ update flow. Invalid transitions now return 409 (`ConflictError`) based on lifecycle FSM rather than allowing convention-only changes. Added extra transition coverage for invalid `In preparation -> Awarded` and valid `Submitted -> Awarded`. | src/controllers/rfq_controller.py; tests/unit/test_rfq_controller.py | `pytest tests/unit/test_rfq_controller.py -q` passes with FSM transition tests including invalid and valid terminal-path cases. | 67cb524 | Landed on `origin/main` via local merge of `origin/fix/lg-01-rfq-status-fsm`. |
| LG-02 | Last-stage completion hardcodes Submitted | RESOLVED | Last-stage advance no longer force-sets RFQ status to `Submitted`; status remains explicit while progress reaches 100. | src/controllers/rfq_stage_controller.py; tests/unit/test_rfq_stage_controller.py | Covered by unit test asserting final-stage completion keeps status unchanged and sets progress to 100. | 4e6fdf9 | Already merged on `origin/main` prior to LG-01 merge. |
| LG-03 | Skipped stages deflate RFQ progress | RESOLVED | Updated progress calculations to exclude `Skipped` stages in both RFQ stage-advance and RFQ terminal transition flows. Progress now reflects non-skipped work and reaches 100 when remaining stages are skipped after completed work. | src/controllers/rfq_stage_controller.py; src/controllers/rfq_controller.py; tests/unit/test_rfq_stage_controller.py; tests/unit/test_rfq_controller.py | `pytest tests/unit/test_rfq_controller.py tests/unit/test_rfq_stage_controller.py -q` → 20 passed. Added tests for skipped-stage exclusion in both controllers. | 8c314b8 | Landed on `origin/main` via merge of `origin/fix/lg-03-lg-04-progress-and-create-status` (PR #15). |
| LG-04 | RFQ create relies on model default status | RESOLVED | RFQ create flow now explicitly sets `rfq_data["status"] = "In preparation"` before datasource create call, removing hidden dependency on model default. | src/controllers/rfq_controller.py; tests/unit/test_rfq_controller.py | Same focused test run (20 passed). Added create-flow test asserting explicit status is passed to datasource. | 8c314b8 | Landed on `origin/main` via merge of `origin/fix/lg-03-lg-04-progress-and-create-status` (PR #15). |
| LG-05 | Date recalculation matches by name | RESOLVED | Updated `_recalculate_stage_dates()` to resolve template durations by `stage.stage_template_id == template.id` as the primary path. Added explicit minimal legacy fallback for rows where `stage_template_id` is null, with inline comment explaining backward compatibility behavior. | src/controllers/rfq_controller.py; tests/unit/test_rfq_controller.py | `pytest tests/unit/test_rfq_controller.py -q` → 9 passed, including a dedicated duplicate-stage-name scenario proving ID-based duration selection. | - | Legacy rows without `stage_template_id` still use temporary name-based fallback until backfill/migration strategy is defined. |
| LG-06 | RFQ code generation not atomic | OPEN | Not started. | - | Pending | - | P2; later in logic hardening. |
| SC-01 | `file_path` exposed in API responses | RESOLVED | Removed `file_path` from public stage/file response schema and replaced it with computed `download_url` (`/rfq-manager/v1/files/{id}/download`). Applied consistently to upload response, stage detail embedded files, and stage file list. Internal DB `rfq_file.file_path` remains unchanged for storage/download resolution only. | src/translators/rfq_stage_translator.py; src/translators/file_translator.py; src/controllers/rfq_stage_controller.py; tests/unit/test_file_controller.py; tests/unit/test_rfq_stage_controller.py | Focused batch `pytest ... test_file_controller.py test_rfq_stage_controller.py ... -q` passed (37 total); assertions confirm `file_path` is absent and `download_url` is present in returned payloads. | - | Existing records still store internal `file_path` in DB by design (non-public). |
| SC-02 | Auth contract mismatch (open endpoints) | RESOLVED | Added explicit, config-controlled V1 auth bypass middleware in app startup flow that injects deterministic demo user context (`request.state.user`). Added startup warning log: `V1: auth bypassed, see rfq_iam_ms integration plan.` Updated IAM/event-bus connector docs to clearly state V1 stub/placeholder status (no active integration). | src/config/settings.py; src/app.py; src/connectors/iam_service.py; src/connectors/event_bus.py; tests/unit/test_app_auth_bypass.py | Focused batch `pytest ... test_app_auth_bypass.py ... -q` passed (37 total); verifies bypass context injection and startup warning emission when bypass is enabled. | - | Bypass is intentionally non-production auth; real enforcement remains pending `rfq_iam_ms` integration. |
| SC-03 | Sort parameter allows arbitrary model fields | RESOLVED | Added strict whitelist in `RfqDatasource.list()` for sort fields: `{name, client, deadline, created_at, priority, status, progress, owner}` with optional `-` desc prefix. Invalid sort field now raises `BadRequestError` (clean 400 path via global handler) instead of silent fallback. | src/datasources/rfq_datasource.py; tests/unit/test_rfq_datasource_sort.py; tests/integration/test_api_layer.py | Focused batch `pytest ... test_rfq_datasource_sort.py test_api_layer.py -q` passed (37 total); covers valid asc/desc, invalid-field rejection, and API-layer 400 error contract. | - | Error message now explicitly lists allowed sort fields; clients using unsupported legacy sort keys must update requests. |
| MD-01 | `rfq_stage.stage_template_id` missing | RESOLVED | Added nullable `stage_template_id` FK on `rfq_stage`, wired RFQ stage creation to persist the source template ID for each generated stage, and added Alembic revision to add column + FK in a migration-safe way for existing data. | src/models/rfq_stage.py; src/controllers/rfq_controller.py; migrations/versions/4b1f8f1d4a5b_add_stage_template_id_to_rfq_stage.py; tests/unit/test_rfq_controller.py | `pytest tests/unit/test_rfq_controller.py -q` → 9 passed with stage-template-ID persistence assertion. Alembic sanity check on disposable SQLite DB: `alembic upgrade head` succeeded through new revision and `PRAGMA table_info(rfq_stage)` confirmed `stage_template_id`. | - | Column is intentionally nullable to keep existing rows migration-safe; historical rows may require optional backfill for full ID coverage. |
| MD-02 | `rfq_history` docstring mismatch | RESOLVED | Aligned `rfq_history` module/class commentary to actual persisted schema and V1 runtime reality (dormant table path, real columns `user_id`/`user_name`, free-form action label semantics). No table redesign or behavioral changes. | src/models/rfq_history.py | Docstring/comment-only alignment; no runtime behavior change required. | - | Table remains dormant in current V1 flow; if write-path is enabled later, docs should be revisited with concrete event taxonomy. |
| MD-03 | `rfq_stage_field_value` docstring mismatch | RESOLVED | Aligned `rfq_stage_field_value` commentary to actual model columns and JSON `value` storage reality, including V1 dormant/schema-level usage note versus active `rfq_stage.captured_data` path. No schema/API redesign. | src/models/rfq_stage_field_value.py | Docstring/comment-only alignment; no runtime behavior change required. | - | If this table becomes active in V2, operational docs should describe write/read flow explicitly. |
| MD-04 | `Reminder.updated_at` missing | RESOLVED | Added `updated_at` to `Reminder` model with project-consistent timestamp behavior (`server_default=func.now(), onupdate=func.now()`). Added Alembic revision to append nullable column safely, backfill from `created_at`, and support downgrade across dialects. | src/models/reminder.py; migrations/versions/9f3c7e21b6ad_add_updated_at_to_reminder.py; tests/unit/test_notification_service.py | `pytest tests/unit/test_notification_service.py -q` → 3 passed (includes focused DB-backed assertion that `updated_at` is populated and advances on update). Migration sanity on disposable SQLite DB: `alembic upgrade head` succeeded and `PRAGMA table_info(reminder)` confirmed `updated_at`. | - | Column is nullable for migration safety; application semantics rely on DB/onupdate behavior, so timestamp precision may vary slightly by dialect. |
| TS-01 | `conftest.py` empty, fixtures missing | RESOLVED | Replaced placeholder `tests/conftest.py` with a minimal shared fixture foundation: in-memory SQLite engine/session, app fixture with `get_db` override, and reusable TestClient fixture. Kept fixture surface intentionally small and aligned with existing test style. | tests/conftest.py; tests/integration/test_api_layer.py | Full suite run passes with shared fixtures active (`pytest -q` → 60 passed). | - | Fixture DB is in-memory and ephemeral by design; tests needing custom wiring can still define local setup explicitly. |
| TS-02 | Duplicate tests in root and unit folders | RESOLVED | Removed true duplicate root-level modules now superseded by stronger/updated `tests/unit/` counterparts (`test_notification_service.py`, `test_pagination.py`, `test_rfq_controller.py`). Retained broader and more current unit/integration coverage files. | tests/test_notification_service.py (deleted); tests/test_pagination.py (deleted); tests/test_rfq_controller.py (deleted) | Full suite remains green after dedup (`pytest -q` → 60 passed), confirming no coverage regressions from duplicate removal. | - | Dedup done conservatively by exact-overlap module names; no unique behavior assertions were removed. |
| TS-03 | Missing lifecycle test coverage | RESOLVED | Strengthened focused lifecycle regression coverage for cleaned logic set by adding skipped-stage average-progress exclusion assertion (not just terminal 100% case), while preserving existing tests for explicit create status, FSM valid/invalid transitions, last-stage no-auto-Submitted, template-ID date recalculation, invalid sort 400 contract, and file response shape (`download_url` without `file_path`). | tests/unit/test_rfq_stage_controller.py; existing coverage in tests/unit/test_rfq_controller.py; tests/integration/test_api_layer.py; tests/unit/test_file_controller.py; tests/unit/test_rfq_datasource_sort.py | Large targeted subset passed (`pytest ...` → 46 passed) and full suite passed (`pytest -q` → 60 passed). | - | API invalid-sort contract test remains dependency-override based (stable and fast) rather than full DB-backed endpoint wiring, consistent with current test style. |
| DC-01 | Endpoint numbering duplicated/gapped | RESOLVED | Reconciled endpoint numbering to a unique 1–31 sequence across route comments/docstrings, README reference mapping, and OpenAPI summaries; also aligned current contract artifacts where touched. | src/routes/rfq_route.py; src/routes/workflow_route.py; src/routes/rfq_stage_route.py; src/routes/subtask_route.py; src/routes/reminder_route.py; src/routes/file_route.py; src/app.py; README.md; docs/rfq_manager_ms_openapi_current.yaml; docs/rfq_manager_ms_api_contract_current.html | Verified by grep scan of numbered route comments and OpenAPI summaries (31 unique numbered summaries present, duplicate #26 removed). | - | OpenAPI path ordering still groups stage file listing before advance in file order; numbering remains globally unique and consistent. |
| DC-02 | Connector docs overstate capabilities | RESOLVED | Verified connector modules already state explicit V1 stub/placeholder scope and do not overstate live integrations; no content change needed in this batch. | src/connectors/event_bus.py; src/connectors/iam_service.py | Manual content verification against DC-02 acceptance text. | - | Residual risk unchanged: connectors remain intentionally non-functional until integration phase. |
| DC-03 | `scripts/running_app` contradicts README | RESOLVED | Removed stale helper script that contradicted current README startup flow (port/image/driver/path mismatch). | scripts/running_app (deleted) | Contradictory instructions removed; README remains single source of truth for local startup. | - | If a scripted startup helper is needed later, regenerate from README and keep parity checks. |
| F-NEW-01 | Seeded RFQStage rows miss `stage_template_id` | RESOLVED | Updated seed-generated `RFQStage` rows to persist `stage_template_id = StageTemplate.id`, matching runtime RFQ creation behavior. | scripts/seed.py; tests/unit/test_seed_runtime_sync.py | `pytest tests/unit/test_seed_runtime_sync.py -q` → 3 passed (includes stage-template-ID persistence assertion). | - | Historical data seeded before this change may still contain NULL `stage_template_id` values. |
| F-NEW-03 | `ReminderResponse` missing `updated_at` | RESOLVED | Added `updated_at` to reminder response schema and translator mapping; synchronized current OpenAPI/contract docs for reminder response/table fields. Added focused unit coverage for translator response payload. | src/translators/reminder_translator.py; docs/rfq_manager_ms_openapi_current.yaml; docs/rfq_manager_ms_api_contract_current.html; tests/unit/test_reminder_translator.py | `pytest tests/unit/test_reminder_translator.py -q` passes; assertion confirms `updated_at` is emitted in response model dump. | - | Field remains nullable for compatibility with historical rows that may predate timestamp population. |
| F-NEW-04 | `src/models/__init__.py` docstring out of sync | RESOLVED | Updated package docstring to reflect all current tables (11 total), including dormant V1 tables. | src/models/__init__.py | Manual doc/schema consistency verification against model package contents. | - | Keep synchronized when tables are added/removed in future migrations. |
| SD-01 | Seed assigns phantom `blocker_description` | RESOLVED | Removed phantom `cur_stage.blocker_description` assignment from seed path; seed now sets only persisted blocker fields (`blocker_status`, `blocker_reason_code`). | scripts/seed.py; tests/unit/test_seed_runtime_sync.py | `pytest tests/unit/test_seed_runtime_sync.py -q` → 3 passed (includes source-level assertion that phantom assignment is absent). | - | None identified; no model/schema changes were introduced. |
| SD-02 | Seed RFQ codes mismatch runtime format | RESOLVED | Replaced seed-only `GHI-XXXXX` code generation with runtime datasource logic (`RfqDatasource.get_next_code`) using IF/IB prefixes. | scripts/seed.py; tests/unit/test_seed_runtime_sync.py | `pytest tests/unit/test_seed_runtime_sync.py -q` → 3 passed (asserts seeded codes match `^(IF|IB)-\\d{4}$`). Practical run: `python scripts/seed.py --scenario=demo --reset --seed=42` (with `PYTHONPATH=.` and SQLite `DATABASE_URL`) completed successfully. | - | Prefix selection in seed remains randomized between IF/IB (deterministic when `--seed` is provided). |
| SD-03 | Terminal seeded RFQs keep current stage | RESOLVED | Ensured seeded terminal RFQs (`Awarded`/`Lost`/`Cancelled`) clear `current_stage_id` and keep `progress=100`, mirroring runtime terminal semantics. | scripts/seed.py; tests/unit/test_seed_runtime_sync.py | `pytest tests/unit/test_seed_runtime_sync.py -q` → 3 passed (asserts terminal RFQs have `current_stage_id is None` and `progress == 100`). | - | Current completed-lifecycle seed path generates Awarded/Lost terminal outcomes; rule still correctly covers Cancelled if introduced later. |
| IF-01 | Missing Dockerfile | RESOLVED | Added minimal production-honest API Dockerfile based on `python:3.11-slim`; installs runtime deps from `requirements.txt`, copies app + migrations, exposes `8000`, and starts `uvicorn src.app:app` on `0.0.0.0:8000`. | Dockerfile | Static verification + Docker build attempt in local environment. | - | Image intentionally keeps startup simple; DB migrations are handled by compose command for local stack. |
| IF-02 | Missing docker-compose.yml | RESOLVED | Added compose stack for `postgres:16` + API service, with correct container-network `DATABASE_URL`, Postgres persistent volume, healthcheck, `depends_on`, and API port mapping on `8000`. | docker-compose.yml | Static verification + compose startup attempt in local environment. | - | Compose command runs `alembic upgrade head` before API startup to keep schema aligned. |
| IF-03 | Missing CI workflow | RESOLVED | Added GitHub Actions CI workflow to run on `push` to `main` and `pull_request` to `main`; installs dev deps, runs `ruff check .`, then `pytest -q`. | .github/workflows/ci.yml | Workflow YAML validated locally by syntax/readability review; runtime behavior verified by running corresponding local commands. | - | CI test job uses SQLite `DATABASE_URL` and `PYTHONPATH=.` environment for deterministic runner setup. |
| IF-04 | requirements mixes prod+dev deps | VERIFIED | Re-verified runtime/dev split and tightened it: moved `Faker` from runtime to `requirements-dev.txt`, kept runtime requirements API-focused, and added `ruff` to dev dependencies for CI linting. | requirements.txt; requirements-dev.txt; README.md | Local dependency + lint/test checks executed after split changes. | - | Running seed scripts locally now requires `requirements-dev.txt` (already documented in README). |
